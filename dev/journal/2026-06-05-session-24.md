# Journal — 2026-06-05 · session-24（階段 R：敘事控制 v0.2 揭露橋接 NR0–NR7）

## 任務
使用者「做階段 R」——開發敘事控制 v0.2 補丁的 8 工單（NR0–NR7），修補 v0.1 selfplay 暴露的核心斷鏈
（玩家調查無法轉成真相進度 → 結局永遠 0/X）+ 另 7 項問題。

## 執行方式（與標準並行流程的差異及理由）
這 8 工單是**單一緊耦合敘事子系統**（全部碰 `core/narrative/` + `core/orchestrator_loop.py`），且有硬序列依賴
（NR1/NR2/NR6 import NR0 的模型；NR3→NR4 共用 ending）。並行 worktree 會持續在共用契約/模型檔衝突——
不符合「contract-first 模組邊界並行」的前提。故由整合者（我，Opus）**依 apply order 逐單施工**，
每單走完整生命週期（pre 快照 → 實作 + 測試 → 驗收 pass → post 快照 --verify pass → done → board）。
mock-first / 防暴雷 / flag-gated 紀律不變。

## 工單（全 done，逐單 post 快照 verify=pass）
- **NR0** RevelationBridge：`core/narrative/revelation.py`（EvidenceEvent/TruthProgress/RevealLedger/RevelationBridge；
  build_ledger_from_bible / build_evidence_mapping 接通 kernel 線索（clue.first_sign…clue.core）↔ bible revelation_pool 碎片；
  不可跳級、需 evidence；recap_from_ledger 算部分進度）。loop：start 種帳本、`_revelation_tick` 每 beat 橋接、
  confirmed 鏡射進 revealed_bible；`build_ending_sequence(reveal_recap=)` + masked render 顯示部分進度。8 測。
- **NR1** NPCChat 敘事控制：`core/narrative/npc_chat_control.py`（NPCChatControlContext/Response/Gate、
  control_context_from_meta、response_to_evidence + cap_to_ceiling）；`build_npc_chat_context(control_ctx=)` 注入母題/上限/答債/動機（無 real_bible）；run_npc_chat flag-gated 接上。6 測。
- **NR2** Answer Debt：`core/narrative/answer_debt.py`（classify_question 5 類 + topic、AnswerDebtTracker、payoff_obligation）；
  loop `_answer_debt_tick` 注入義務 + 存 game_meta；npc-chat 依 topic 取債等級。6 測。
- **NR3** EndingGate Surface Variant：`ending_gate.classify_ending_surface`（clean/ambiguous/failed/death/truth_locked；
  **0 confirmed 永不 clean**）；ending.py 加表層收尾文字，僅在 loop 標 escape_quality 時生效（flag OFF 不變）。4 測。
- **NR4** Escape Commit Gate：`core/narrative/escape_commit.py`（兩段式：await_commit→commit）；
  loop `_escape_commit_tick` 注入出口發現義務 + 標記，escape attractor 在 await_commit beat 被延遲。5 測。
- **NR5** Motif Cooldown：`core/narrative/motif_tracker.py`（MotifTracker + extract_motifs + stagnant_motifs）；
  loop 換場景重置、注入 blocked motifs、QualityGate 加 stagnant_motif 告警。5 測。
- **NR6** Motive Heartbeat：同檔 MotiveHeartbeat；loop `_motive_heartbeat_pre`（逾期注入「換管道、不重複句」義務）+ `_motif_motive_post`。4 測。
- **NR7** SurfaceTextSanitizer：`core/narrative/sanitizer.py`（非字母邊界比對，避免誤砍英文單字；
  授權 432.7/17Hz + 契約母題；移除內部標記/壞 fence）；loop `_sanitize_surface` + npc-chat 後端消毒（flag-gated，D4）。6 測。

## 新契約
`dev/CONTRACTS.md §十四`（10 條，已於 session-23 凍結；本 session 落地，未改契約）。

## 整合驗收（階段 barrier）
`tests/test_narrative_v2_integration_nr.py`：用真實 BeatLoop + FakeCaller（revelation_pool 3 碎片）驗證——
1. 開局種子化帳本（3 truths 全 hidden）；
2. 調查數 beat 後 **hinted_or_better ≥ 1**（斷鏈修好，不再 0/X），game_meta.reveal_ledger 同步；
3. 結局 recap.partial.found ≥ 1 且 ending_surface == ambiguous_escape；
4. **flag OFF：不建帳本、不標表層變體，行為與現況一致**。

## 驗證
- 全套件 **679 passed, 3 skipped**（+44 測：8 NR 單元 + 3 整合）。flag OFF 與 flag ON 各跑一次皆全綠。
- board --check：**70 工單 0 errors 0 warns**；8 個 NR*__post 快照 verify=pass。
- 防暴雷不變：bridge 只決定等級不洩 content；npc-chat context 無 real_bible/secret_core（整合測斷言）。

## 設計同步
`design-fixed/13-narrative-control-v2.md` + `09-revision-notes.md` 補丁六狀態改「已落地、已測（679 passed）」。

## 現況 / 下一步
- **70 工單全 done**（MVP-A + S/P/B/N/C/R）。階段 R 把 v0.1 點出的「調查→真相」斷鏈正式接通。
- 建議下一步：用真實 LLM 跑一場 flag-ON selfplay，對照 v0.1 那場 0/7，確認實機 recap 不再全 0（單元/整合已證機制，缺真模型體感驗證）。
