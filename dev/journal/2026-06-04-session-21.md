# Journal — 2026-06-04 · session-21（開發階段 N：敘事控制 NC0–NC7）

## 完成 ✅ — 階段 N 8/8（NC0–NC7），整體 57 工單全 done
依使用者「開始開發，做階段 N」，按 apply order **NC0→…→NC7** 逐單跑完整生命週期
（pre 快照 → mock-first 實作 → 驗收 pass → post 快照 --verify pass → 回填 last_good → done）。
移植 patch reference_code 演算法到 `core/narrative/`，全程 **flag `ENABLE_NARRATIVE_CONTROL` 預設 OFF**（旁路、不破壞現況）。

## 工單
- **NC0** 契約凍結 + flag：constants `ENABLE_NARRATIVE_CONTROL`（OFF）+ `core/narrative/models.py`（NarrativeContract 系列）。3 測。
- **NC1** Narrative Contract：`contract.py` 從 real_bible/protagonist 組契約（動機/核心問題/budget/ladder）；loop start flag-gated 建+存。5 測。
- **NC2** Opening Director：`opening_director.py` 從契約挑 ≤budget 開場元素（主要 ≤3、最多 hinted）；接 `_kernel_intro_beat`，refine UB6；不洩 hidden_truth。4 測。
- **NC3** Story Agent Downgrade：`story_control.py` 加 allowed/forbidden_new_elements + beat_purpose + truth_reveal_limit + player_motive + element_limit=1；SKILL Delta 段；接 `_step_kernel`。4 測。
- **NC4** Reveal Ladder：`reveal_manager.py`（hidden→…→actionable，不跳級、evidence-gated、開場 hinted、一次升一階）；loop 依線索數漸進。6 測。
- **NC5** Quality Gate：`quality_gate.py`（元素過載/forbidden/缺動機/reveal jump/無意義選項 + repair instr）；loop 非阻斷記錄。6 測。
- **NC6** Ending Gate：`ending_gate.py`（clean_escape 需 explicit_escape+exit_reached+threat_resolved；**0/8 不可 clean**）；接 attractor escape，標 escape_quality；refine UB7。6 測。
- **NC7** Config Hooks（optional）：`config.py`（safe 預設 + config-中心數值覆寫 + overrides）；contract 吃 config。4 測。

## 新契約
`dev/CONTRACTS.md §十二`（patch v0.1）：NarrativeContract / OpeningBlueprint / RevealLadder / StoryAgentDelta / QualityGate / EndingGate。board.py 加 stage N→10；PARALLEL-PLAN 接 N。

## 驗證
- 全套件 **605 passed, 3 skipped**（+38 測）；每單收尾全回歸綠燈（loop 多處 flag-gated 接點不破既有）。
- board --check：**57 工單 0 errors 0 warns**；8 個 NC*__post 快照 verify=pass。
- flag OFF 預設 → 行為與現況完全一致（旁路）。

## 與前批的關係
此 patch **校正** UB6（開場元素過載 → Opening Director 收斂 ≤budget）與 UB7（結局 → Ending Gate 加因果門檻、0/8 不可 clean escape）。UB6/UB7 路徑於 flag OFF 時保留。

## 現況
57 工單全 done（MVP-A + 階段 S/P + MVP-B UB1–UB7 + 階段 N NC0–NC7）。
敘事控制層就緒但預設 OFF——要啟用：`ENABLE_NARRATIVE_CONTROL=true`（.env/config）。
