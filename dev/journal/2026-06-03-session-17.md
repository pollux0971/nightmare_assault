# Journal — 2026-06-03 · session-17（新增階段 P：配置中心 / Story Agent 模組化）

## 動作 — 依 patch `nightmare-assault-story-doc-patch-batch-v1.1` 建工單，不寫遊戲碼
使用者要求：依 docs-first patch batch v1.1「更新相關 story（設未處理）/ 生成 story，新增一個階段，把 MVP-A 部份寫完整」。

## 決策
- patch v1.1 是疊在「已 done 的 MVP-A（U00–U20）+ 階段 S（SK01–SK13）」之上的 **additive 新增能力**（配置中心 / Story Agent 模組化，保留 static fallback）。
- **不回滾任何已 done 的 U/SK 工單**（避免破壞 DoD / known-good 快照不變式）。改採「**新建階段 P + 工單 P0–P7，狀態全 todo（未處理）**」，以 `depends_on` 指向相關 done 工單（U05/U13/SK04/U18）。同時滿足「相關 story 設未處理」與「生成 story」。
- 沿用階段 S 先例（hand-built，不動原始 `gen_stories.py` 的原計畫資料表）。

## 變更
- 新增 8 工單：`dev/stories/P0–P7.md`（apply order P0→P1→P2→P3→P4→P6→P5→P7）。
  - P0(A) 契約凍結｜P1(B) 配置表 additive SQLite｜P2(C) PromptComposer 決定性+hash+preview｜P3(D) Story fragment 化(kernel obedience/反重複/自由選擇)｜P4(D) runtime config-first+static fallback｜P6(F) run 快照+prompt 回歸測試｜P5(E) 配置 UI(draft→preview→activate)｜P7(D) 擴展其他 agent。
- 新增 `dev/stages/stage-P.md`（進入條件/目標/apply order/工單表/鐵律）。
- `dev/CONTRACTS.md` §十一（新增，additive）：ConfigSchema/PromptFragments/PromptComposer/AgentContextPolicy/FeatureFlags/ConfigPromptSource/RunConfigSnapshot/ConfigUI，canonical 指 patch docs（**未動既有 FROZEN 契約**，比照 §十 kernel 補丁先例）。
- `dev/tools/board.py`：`_stage_num` 加 S→8、P→9（B→7 之後依序）；STAGE_TITLE 補 S/P 標題。
- `dev/PARALLEL-PLAN.md`：§二 排程表補 S/P 列、§三索引補 S/P + 階段索引含 stage-S/stage-P。

## 驗證
- `board.py` 重算：47 工單；`board.py --check` → **OK、0 errors、0 warns**（新 8 契約全部解析得到，無 dangling）。
- 階段 P 渲染於 STATUS 末段（MVP-B、階段 S 之後），8/8 todo。
- 未寫任何遊戲 runtime 程式碼（純工單/契約索引/看板工具）。

## 現況
MVP-A（U00–U20）+ 階段 S 全 done（34/47）。新增階段 P（P0–P7）全 todo，可依 PARALLEL-PLAN §四「做階段 P / 做工單 P0」認領；起點 P0（無前置）。剩 MVP-B（UB1–UB5）+ 階段 P（P0–P7）。
