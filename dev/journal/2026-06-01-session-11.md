# Journal — 2026-06-01 · session-11（階段 5：beat 主迴圈）

## 階段 5 — 完成 ✅（1/1，整體 16/25，MVP-A 16/20）— **後端 MVP-A 核心通**
`U15` 由整合者（我，Opus，lane A）親自實作，整合風險最高、不並行。

### core/orchestrator_loop.py — BeatLoop
- `start(opts)`：run_setup 生世界（B8 不降級）→ create_run → 第一個 beat（orchestrator + story）。
- `step(player_decision)`：warden → event 抽取 → orchestrator（揭露）→ story（串流）→ 安全點。
- `_safe_point`：落 SQLite 快照（含當時 rolling_summary + blackboard 全快照）→ 修剪 beat_window（系統級記憶管理）→ compactor.compact(evicted) → apply_to_blackboard(submit_patch) → merge_and_bump（A5 安全點才生效）→ 發 BEAT_COMPLETED。
- 信號：RULE_VIOLATION/ENDING_TRIGGERED/SKILL_CLAIMED/CONTEXT_THRESHOLD/BEAT_COMPLETED。

### 整合期再修一個真實 gap（retry-with-repair）
- 真實連續遊玩 demo 首跑：setup 的 LLM 把 NPC `personality` 填成「疏離」（非 5 列舉值），Pydantic 驗證失敗。
- **修正**：`SkillCaller.call` 加 retry-with-repair（08 §三）——驗證失敗時把錯誤回饋給模型再試一次。所有結構化 agent 受惠。U07 重新 known-good。

### 🎮 真實連續遊玩跑通（用 test_api.txt 的 key）
- setup（claude-3.5-sonnet）生廢棄精神病院世界 → Beat1 開場決策 → Beat2 玩家選「推門」story 連貫接續 → Beat3 玩家**自由打字**「搜查護理站找門禁卡」story 合理回應（護理站/櫃子聲響）。
- 3 beat 全落 SQLite；防暴雷全程維持（story 只讀 revealed，real_bible 鎖在 blackboard）。
- **這證明後端 MVP-A 核心（setup→warden→orchestrator→story→快照→compactor）端到端可玩。**

### 驗證
- U15 mock 整合測試 6 passed（含 window 有界、compaction、warden 結束、信號、防暴雷 context）；全套件回歸綠燈；board --check 25 工單 0 errors。

## 現況
- 進度 16/25（階段 0–5 done）。**只剩階段 6 前端（U16–U19）就是完整 MVP-A。**
- 下一步：**階段 6（前端 + MVP-A 打磨）**：E:`U16` pywebview 骨架+API → `U17` 串流渲染 → `U18` 主畫面+前置 → `U19` 存檔+道具。完成 = MVP-A 六項驗收。
