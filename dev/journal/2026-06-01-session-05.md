# Journal — 2026-06-01 · session-05（階段 0：專案底座）

## 階段 0 — 完成 ✅
單軌（foundation，main 上直接做，主 agent Opus）。story：`E0-S0`。

### 生命週期
- pre 快照：`E0-S0__pre__20260601-104936`
- 實作 → 驗收 pass → post 快照：`E0-S0__post__20260601-105131`（9 檔，verify OK，known-good）

### 產出
- `pyproject.toml`：name/version、deps(pywebview/httpx/PyYAML)、dev(pytest)、[tool.pytest.ini_options]、setuptools find=core*。
- `conftest.py`：根上 path、共用 fixtures 佔位。
- `core/__init__.py`(+version) 與 5 個子套件 `__init__.py`（agents/state/memory/llm/persistence）。
- `tests/test_smoke.py`：3 測（core 可匯入、子套件可匯入、框架存活）。
- `.venv`（gitignored）＋ pytest 9.0.3。

### 驗收（整合 gate）
- `.venv/bin/python -m pytest -q` → 3 passed, **exit 0** ✓
- `.venv/bin/python -c "import core"` → OK 0.0.0 ✓
- board --check：73 stories、0 errors、0 warns；無 in_progress 殘留。

### 順手修正
- **snapshot.py TRACKED 補入 `tests/` 與 `conftest.py`**——否則測試底座不會進快照、回滾還原不到。
  已驗證 post 快照含這 9 檔。
- 修正 E0-S0 驗收用詞：pytest 0 測會回 exit 5，故附 smoke 測讓綠燈為真實 exit 0；驗收改用 `.venv` 指令。

## 狀態
- E0 進度 1/7；CONTRACTS 仍 DRAFT（階段 2 收尾才凍結）。
- 下一步：**階段 1（E0 基礎設施）**——批次①並行 E0-S1/S2/S5/S6 → E0-S3 → E0-S4。
  仍為 foundation 單軌；多 worktree 真並行從階段 5 起。
