# Journal — 2026-06-01 · session-06（dev/ ⇄ design-fixed/build/parallel-dev-plan 對齊重構）

## 背景
使用者交付修訂設計 `design-fixed/`（新增 07/08/09 + build/）與 `parallel-dev-plan.md`，成為新施工真相。
舊 dev/ OS（73 stories/E0–E17/抽象契約）與之衝突。已拍板：build/+parallel-dev-plan 為主、改造 dev/ 執行、
保留快照回滾+board、MVP-A/B 收斂、序列為主保留並行基建。

## 變更
- **歸檔**：舊 73 stories + 21 epics → `dev/_legacy/`（出 board 掃描）。
- **工單化**：`gen_stories.py` 換資料表 → **25 工單**（MVP-A U00–U19 + MVP-B UB1–UB5），含 lane(A–F)/stage(0–6,B)/depends_on/contracts/acceptance(BUILD-PLAN+CHECKLIST)/rollback。生成 `dev/stages/`（8 階段索引）。
- **board.py**：分組改 stage、顯示 lane；**新增前向依賴 lint**（depends_on 的 stage 不得 > 自身）+ 工單檔解析守衛。
- **CONTRACTS.md**：重寫為**契約索引（FROZEN）**，31 個 id 指向 build/CONTRACTS+07/08+01 為 canonical（單一真相，不複製）。
- **PARALLEL-PLAN.md**：重寫為 7 階段 × 6 工線 + 工單映射 + MVP-A 六項驗收 + 模型策略 + 執行機制。
- **WORKFLOW.md**：重寫為 contract-first 模組邊界並行、5 原則、工線編制、git 分支(main/develop/integration/feature-*)、禁止事項。
- **ROLLBACK.md**：保留快照機制，術語 story→工單、FROZEN、track→工線。
- **CLAUDE.md**：施工真相指向 build/+parallel-dev-plan；工單生命週期；工程鐵律(C2/E2/C3/D4/B9/A3/A4/A5/B8)；分支禁止事項；模型策略 U08/U14。
- **新增 `dev/SOURCES.md`**：四組文件單一真相歸屬。
- **承接**：原 E0-S0 地基 → `U00` done（新 post 快照 `U00__post__20260601-120853`，並存舊 `E0-S0__post__…`）。

## 驗證
- `board.py --check`：25 工單、0 errors、0 warns、無漂移。
- 階段分佈：0:1✅｜1:5｜2:4｜3:3｜4:2｜5:1｜6:4｜B:5 = 25。無前向依賴（board lint 自動驗）。
- 契約：每個工單 `contracts` 都對得到 CONTRACTS.md。
- 快照工具回歸正常；U00 done + known-good 快照保留。

## 現況
- 階段 0 done。CONTRACTS FROZEN。下一步：**做階段 1 / 做工單 U01**（Pydantic models，工線 B）。
- 真並行從階段 1 起（同階段不同工線 worktree + 子 Sonnet）；承重牆 U08/U14 升 Opus。
