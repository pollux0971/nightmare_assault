# Journal — 2026-06-01 · session-08（階段 2：LLM 基礎 + agent 外殼）

## 階段 2 — 完成 ✅（4/4，整體 10/25）

### 工單
- `U06` OpenRouterClient（工線 C）：call/stream + fallback 鏈 + timeout + llm_traces；可注入 httpx transport 供測試。
- `U08` StreamParser（工線 C，**承重牆**）：滑動視窗偵測分隔符（可被拆多 token）、narrative/decision 分離、三級 repair（L1 程式碼/L2 LLM/L3 fallback UI）、narrative 不回收。
- `U11` event 抽取（工線 D）：純規則函式，四型事件 + merge_with_self_report（衝突信程式碼 C12）。
- `U07` SkillCaller + SkillLoader（工線 C）：載 SKILL.md（mtime 熱重載）→組 prompt→呼 client→Pydantic 驗證。

### 子 agent 失敗 → 改 inline
波次 2a 三個 Sonnet 子 agent 同時被「socket connection closed」中斷（環境連線問題，非工作失敗）。
U06 的 client.py 已由子 agent 寫好（高品質、完整），其餘中斷。為求可靠，整合者（我，Opus）**inline 完成**
U06 測試、U08 parser+測試、U11+測試、U07+測試。模型策略上本應 Sonnet 子 agent，但 dispatch 不穩時以可靠交付為先。

### API key 安全處理
- 使用者的共用 `test_api.txt`（含多服務 live key：OpenAI/Google/OpenRouter/Sentry）。
- 已加入 `.gitignore`（`test_api.txt`/`*api*.txt`/`*.key`）與 snapshot `EXCLUDE_FILES`，**永不進 git/快照**。
- **真實 OpenRouter smoke 測試通過**：用 env 注入 sk-or-v1 key（從不寫入任何檔/不外洩），openai/gpt-4o-mini 回應成功。U06 真 API 驗證 ✓。

### 驗證
- 各工單測試綠燈：U06 7 passed+1 skip(real smoke 預設)、U08 9、U11 9、U07 9。真 smoke 另跑 1 passed。
- 全套件回歸綠燈。board --check 25 工單 0 errors。
- SkillLoader 讀到 8 個真實 prompt（setup/story/warden/…，已從 design-fixed/skills 複製進專案 skills/）。
- 依賴：.venv 加裝 httpx 0.28.1；pyproject 已宣告。

## 現況
- 進度 10/25（階段 0–2 done）。下一步：**階段 3（核心 agent 真接）**：D:`U09` setup、`U10` orchestrator、`U12` warden。
- 階段 3 起會真打 LLM（setup 生世界、orchestrator 揭露、warden 裁決）；測試用 mock client 為主，真 smoke 用 test_api.txt 的 key（env 注入）。
- 提醒：子 agent dispatch 本 session 不穩；若仍不穩，續以 inline 完成。
