# Journal — 2026-06-04 · session-22（MVP-C：聊天室 + 離場命運 MC1–MC5）

## 完成 ✅ — 階段 C 5/5（MC1–MC5），整體 62 工單全 done
依使用者「完成 MVP-C」，界定範圍＝接通兩個既有但未接迴圈的 agent（npc-chat + offstage-fate），
按工作流建工單上板（62 工單）→ 逐單跑完整生命週期。

## 工單
- **MC1** NPC-Chat agent + 持久化：`core/agents/npc_chat.py`（認知卡投影，**無 real_bible/secret_core**，self_aware 只當旗標；`<player_action>` 包覆）；db `add_chat_log/load_chat_logs`。5 測。
- **MC2** 聊天退出濃縮：`condense_chat`（規則版 3–4 句）+ `apply_chat_exit`（寫 recent_chat_digest 進 story hot context，發 CHATROOM_CLOSED）。4 測。
- **MC3** 聊天室 UI + API：webview `list_present_npcs/open_chatroom/send_chat/close_chatroom`（不洩 secret）；前端 💬 + `dlg-chat`（NPC 列表/對話/退出）。4 測 + JS。
- **MC4** Offstage-Fate agent：`offstage_fate.py` `roll_fate`（依 alignment 加權擲骰，程式碼決定型別）+ `run_offstage_fate`（LLM 寫血肉 → 只寫 npc/scene patch，C6 碰不到 secret_core）+ `offstage_fate_tick`（低頻、僅離場 NPC）。8 測。
- **MC5** 離場隱藏 + 重逢揭曉：db `offstage_fates` 隱藏 table；`store_hidden_fate` / `reveal_carried_fragment` / `check_reunions`——離場期間命運敘事/碎片對 story 完全隱藏，NPC 重新 present 才把 carried_fragment 過揭露閘門進 revealed_bible。5 測。

## 新契約
`dev/CONTRACTS.md §十三`（MVP-C）：`NPCChat` / `OffstageFate`。board.py 加 stage C→11；PARALLEL-PLAN 接 C。

## 驗證
- 全套件 **631 passed, 3 skipped**（+26 測）；每單收尾全回歸綠燈。
- board --check：**62 工單 0 errors 0 warns**；5 個 MC*__post 快照 verify=pass。
- 防暴雷：npc-chat 認知卡與 offstage 隱藏命運皆結構性不洩 real_bible/secret_core/碎片內容。

## 現況
**62 工單全 done**（MVP-A + 階段 S/P/B/N + MVP-C）。
世界活起來了：可跟在場 NPC 多輪對話（退出濃縮進故事）、離場 NPC 有四種命運（隱藏到重逢/搜屍才揭曉）。
聊天室 💬 已接前端；offstage-fate tick 旁路（需 NPC 離場觸發）。
