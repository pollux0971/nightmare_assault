# Journal — 2026-06-01 · session-07（階段 1：核心資料層，無 LLM）

## 階段 1 — 完成 ✅（5/5）
contract-first 模組邊界並行：主 agent（Opus，我）當整合者；工單交 **Sonnet 子 agent**，我驗收+快照+整合。
全在 main 樹（工單檔案範圍互斥），依依賴波次執行。

### 波次與結果
- **1a 並行**：`U01` Pydantic models（工線 B，58 測）∥ `U02` constants+SignalBus（工線 C，46 測）
- **1b**：`U03` Blackboard+版本/patch+權限（工線 B，97 測）
- **1c 並行**：`U04` 場景系統（工線 B，28 測）∥ `U05` SQLite（工線 B，19 測）

### 親自驗證的關鍵不變式
- **權限（C6/C2）**：dreaming 碰 secret_core→PermissionError；story 寫 npc_evolving→PermissionError；compactor/orchestrator/story 碰 real_bible→擋；dreaming 可寫既有 NPC evolving。
- **並行 patch（A4/A5）**：pending patch merge 前 snapshot 不可見；過期 base_version 被丟棄、有效套用、version+1。
- **回溯（A3）**：SQLite load beat10 取回『當時』摘要 S10，非 beat30 的 S30。
- **default_factory（A0）**：BeatMeta 預設 list 非共享。
- 全測試套件回歸綠燈（smoke + U01–U05）。

### 產出檔
core/models.py、core/constants.py、core/signal.py、core/blackboard.py、core/scene.py、core/persistence/db.py
＋對應 tests/*；pyproject 補 `pydantic>=2.0`；.venv 裝 pydantic 2.13.4。

### 快照（known-good）
U01–U05 各有 post --verify pass 快照，last_good_snapshot 已回填；U01/U02、U04/U05 各有 pre（波次還原點）。

## 現況
- 進度 6/25（階段 0+1 done）。board --check 0 errors。CONTRACTS FROZEN。
- 下一步：**階段 2（LLM 基礎 + agent 外殼）**：C:`U06`→`U07`、`U08`(承重牆，升 Opus)｜D:`U11`。
  此階段首次碰真 API（U06）；承重牆 U08 StreamParser 建議用 Opus 子 agent。
