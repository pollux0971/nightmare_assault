# Journal — 2026-06-05 · session-25（階段 H：Runtime Hard-Gate v0.3.1 HA1–HE1）

## 任務
使用者依 `nightmare-assault-runtime-hardgate-patch-v0.3.1` 要求：新增 story + **單線程開發、盡量不要出錯**。
把已存在但「半接」的 Narrative Control 模組從 monitor 升級成真正 runtime hard-gate（pass/reject/repair once/fallback）。

## 執行方式
單線程（整合者 Opus 逐單），每單完整生命週期：pre 快照 → 實作 + 測試 → 全套件綠 → post 快照 verify=pass → 回填 → board。
每批（A/B/C/D/E）末跑 agent_play 冒煙（APPLY_ORDER 要求）。不加 story/lore/agent。

## 工單（9/9 done，逐單 post 快照 verify=pass）
- **Batch A 結局/反劇透**
  - HA1 Ending Observation Invariant：`_enforce_ended_invariant`（ended ⇒ dp 無 options/free_input_hint）+ agent_play 輸出層保險。
  - HA2 Death Causality Guard：`core/narrative/ending_causality.py`（EndingCandidate + EndingCausalityGate）；loop attractor 的 death←danger 無死因 → 降級（**禁止 danger→death**）。
  - HA3 Hidden Recap Masking：`public_recap`（found + hidden_count + 遮罩標題，無 hidden content）；agent_play 預設遮罩，`--debug-reveal-truth` 才 full。
- **Batch B 調查→Evidence→Reveal**
  - HB1 Story Evidence Extraction：`core/narrative/evidence_extractor.py`（調查型 + 具體資訊 + reveal 無變化 → 保底 hinted evidence；map 不到 → fallback/unmapped）；EvidenceEvent 擴充 player_facing/debug_reason。
  - HB2 RevelationBridge Unified Inputs：`_revelation_tick` 回 changed、串 `_story_evidence_tick`；step 暴露 reveal_updates / evidence_events_this_beat / unmapped_evidence_this_beat。
- **Batch C NPC runtime gate**
  - HC1 NPCChat Structured Gate：`run_npc_chat_structured` → 解析結構化 NPCChatResponse → NPCChatControlGate.validate → repair once → `safe_fallback_reply`；evidence 封頂 → bridge；解析失敗保底純文字。webview send_chat 接上。修 NR1「NPC 走 bridge 但靠掃字」軟點。
- **Batch D 品質/表層 repair**
  - HD1 QualityGate Repair Once：`core/narrative/quality_repair.py`（StoryRepairPipeline）；loop `_run_story_with_repair`（check→repair once→deterministic fallback，最多一次）+ `_deterministic_story_result`（安全選項）。
  - HD2 Surface Sanitizer All Outputs：sanitizer 加 path/IP/權限/存取權/系統提示/core/access + `scan`/`sanitize_options`/`has_severe`；loop `_sanitize_decision_point`（選項+recap）；agent_play ending rendered_text 消毒。
- **Batch E 可觀測**
  - HE1 Observation Debug Fields：agent_play observation 加 `debug`（committed_event/progress_delta/escape_step/evidence_events/unmapped/reveal_updates/quality_gate/model_used）+ NPC 三分層（visible/known/chat_available）；debug 只露 truth_id、不洩 content。

## 新契約
`dev/CONTRACTS.md §十五`（9 條）：EndingObservationInvariant / EndingCausalityGate / HiddenRecapMask /
StoryEvidenceExtractor / EvidenceEvent(擴充) / NPCChatResponse(結構化) / StoryRepairPipeline /
SurfaceSanitizer(擴充) / ObservationDebug。board.py 加 H→13；PARALLEL-PLAN 接 H。

## 踩到並修正的錯（單線程的價值）
- HD1 後 11 個測試紅：`_run_story_with_repair` 用了 `ENABLE_NARRATIVE_CONTROL` 但該方法沒 local import
  （loop 慣例是各方法 local import 此 flag）→ NameError → kernel step fallback legacy。補 import 後 705 綠。

## 驗證
- 全套件 **714 passed, 3 skipped**（+19 測）；**flag OFF 與 flag ON 各跑一次皆全綠**。
- board --check：**79 工單 0 errors 0 warns**；9 個 H*__post 快照 verify=pass。
- agent_play 冒煙：ended observation 無 options、debug 欄位齊全、NPC 三分層、無 hidden content 洩漏。

## 設計同步
`design-fixed/14-runtime-hardgate.md`（新）+ `09-revision-notes.md` 補丁七，狀態改「9/9 done、已測（714 passed）」。

## 現況 / 下一步
- **79 工單全 done**（MVP-A + S/P/B/N/C/R/H）。Narrative Control 各 gate 皆為真正 hard-gate。
- 建議：用真 LLM 跑一場 flag-ON selfplay（`dev/tools/agent_play.py --flag --jsonl-log ... --max-beats 20`），
  驗 ended 無 options / danger 不致死 / 調查推進 / NPC 結構化不亂生 lore / 無洩漏，並由 jsonl 斷言彙整。
