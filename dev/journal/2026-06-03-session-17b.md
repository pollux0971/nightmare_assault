# Journal — 2026-06-03 · session-17b（執行階段 P：配置中心 / Story Agent 模組化）

## 完成 ✅ — 階段 P 8/8（P0–P7），整體 47 工單 42 done
依使用者「做階段 P」+ 認可 8 個配置中心契約，按 apply order **P0→P1→P2→P3→P4→P6→P5→P7** 逐單跑完整生命週期
（pre 快照 → 實作 mock-first → 驗收 pass → post 快照 --verify pass → 回填 last_good → done）。單一 session 序列鏈，
工作於 master，回滾骨幹用 dev/snapshots/（每單 pre/post）。

## 工單
- **P0** 契約凍結：`config/CONTRACT_FREEZE.md`（agent/profile/fragment-key/flags/fallback 政策）；無 runtime 變更。
- **P1** 配置表 additive：`core/config/schema.py`（10 表 + ConfigStore）+ `fragments.py`（fragment 文字庫單一來源）；
  Database._init_schema 掛 additive migration（CREATE IF NOT EXISTS + INSERT OR IGNORE，舊存檔不破壞）；種子 mvp_a_safe + story 八 fragment + 政策（include_real_bible=0）。9 測。
- **P2** PromptComposer：`core/config/composer.py` 決定性編譯（sort_order/enabled/變數代入/穩定 prompt_hash/preview 零 LLM/缺必填變數拋）。11 測。
- **P3** Story fragment 化：`core/config/story_prompt.py`（compose_story_prompt + validate + 變數面對齊 docs/03）+ `story.assert_respects_forbidden`（反重複守門）。開門回歸過、context 無 real_bible。8 測。
- **P4** Runtime 整合：`core/config/flags.py`（.env>active>default>hardcoded）+ `runtime.py`（ConfigPromptSource，active→default→static fallback）；SkillCaller/run_story 加 system_override；BeatLoop 4 處 story 呼叫接 config-first（flag off/失敗→static，承重牆不破）。30-beat ON/OFF 不崩。9 測。
- **P6** Run 快照 + 回歸：BeatLoop._snapshot_run_config（preview 零 LLM 存 run_config_snapshots）；`test_prompt_regression`（開門/無暴雷/hash 決定/new clue/free input 五項）。9 測。
- **P5** 配置 UI：webview API（config_overview/list_fragments/save_prompt_draft/preview_prompt/activate_prompt_draft/flags/profile）+ `dlg-config` 前端（draft→preview→activate；preview 零 LLM）；draft 隔離（存 prompt_fragment_versions，不動 active 直到 activate）；composer overrides。node JS 檢查過。10 測。
- **P7** 擴展其他 agent：warden/orchestrator/compactor/setup 各 fragments+binding+config+context policy；run snapshot 涵蓋全 agent；real_bible 可見性 story=0/orchestrator=1/setup=1/warden=0/compactor=0。10 測。

## 驗證
- 全套件 **524 passed, 3 skipped**（基線 +~66 新測）；每單收尾跑全回歸綠燈（承重牆 base/story/beat-loop 改動不破既有）。
- board --check：47 工單 0 errors 0 warns；8 個 P*__post 快照皆 verify=pass。
- 契約：dev/CONTRACTS.md §十一（8 契約，additive，未動既有 FROZEN）；CONTRACT_FREEZE.md 凍結名稱。

## 設計要點
- **additive only**：配置表掛既有 DB 連線、migration 冪等、舊存檔可讀；config 失敗一律退 static SKILL.md，不崩 MVP-A。
- **決定性**：PromptComposer 同 fragments+變數→同 prompt_hash；preview 純函式零 LLM。
- **draft 安全**：draft 只進 prompt_fragment_versions，active 編譯 prompt 不受影響直到 activate。
- **防暴雷不退化**：story include_real_bible=0（context policy + build_story_context 雙保險），orchestrator/setup 才可讀。
- ProgressKernel 仍是 world-state 真相；story 只 realize（SKILL.md 規則已外部化為可配置 fragment，行為等價）。

## 現況
MVP-A（U00–U20）+ 階段 S（SK01–SK13）+ 階段 P（P0–P7）全 done（42/47）。剩 MVP-B（UB1–UB5）。
配置中心已就緒但 ENABLE_CONFIG_CENTER 預設 OFF（static 行為不變）；要啟用：設定 .env 或配置中心 UI 切 flag。
