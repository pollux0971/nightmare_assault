# Journal — 2026-06-02 · session-16（敘事規劃強化：6 條強制屬性）

## 完成 ✅ — 階段 S 13/13（SK10–SK13），整體 38 工單 33 done
依使用者要求，強制 kernel 遵守 6 條敘事規劃屬性。

## 工單
- SK10 **多出口 + 多解法**：`core/graph_invariants.py`（INV1 每場景≥2事件、INV2 起始≥2出口、INV3 離場≥2解法、INV4 gateway≥2intent、INV5 每事件有後果）。
  build_themed_graph 改 3 場景 9 事件（exit_start/exit_alt/search_start｜meet_npc/descend/mid_escalate｜deep_find/deep_retreat/deep_escalate），末端強制 validate。8 測。
- SK11 **每個行動有後果 + sparse fallback**：`_make_fallback_event` context-aware（缺線索→種線索／缺NPC→痕跡／否則升壓）；任意越界輸入皆 ≥1 delta。5 測。
- SK12 **soft lookahead（可重算）**：ProgressResult.soft_lookahead 每 beat 重算（預覽落地後場景的其他事件），餵 story context 標示非既定；不持久於 GameState。4 測。
- SK13 **attractor-based ending**：`core/attractors.py` evaluate/dominant_ending（death←danger、truth←碎片、escape←線索+存活+核心）；loop 每 beat 評估，越門檻觸發結局（via=attractor），與 warden 結局並存。5 測。

## 驗證
- kernel-only demo（船艙主題）：每場景 3 事件、exit_start/exit_alt 雙出口、每 beat 有 delta、fallback 在越界時種線索/升壓、soft_lookahead 逐 beat 重算、b10 escape 吸引子(pull=1.0)觸發結局。
- 全套件回歸綠燈；board --check 38 工單 0 errors；每工單 known-good 快照。

## 設計
- 多出口/多解法靠 graph 模板 + invariants 強制（builder 末端 assert）。
- attractor：結局是吸引子（拉力越門檻），不是固定終點節點；門檻在 constants（DANGER_DEATH/TRUTH_FRAGMENT/ESCAPE_CLUE）。
- 更深的主題專屬分支圖（非單一模板）仍留 Patch 3。

## 現況
Narrative Progress Kernel + 主題化 + 6 條敘事規劃屬性 全部落地。剩 MVP-B（UB1–UB5）。
