# Journal — 2026-06-01 · session-02（穩定性/回滾稽核）

## 本次工作
依使用者要求，再次稽核工作流穩定性與回滾機制，並釐清 design/skills 的定位。

### 釐清
- `nightmare-assault-design/skills/` = **遊戲執行期 agent prompt**（被實作的對象），非開發工作流。
  已在 CLAUDE.md §8 明文標註，避免未來 session 混淆。dev OS 本來就正確地把它們當遊戲 artifact
  （E0-S5 loader + 各 agent 接線 story 處理），無需改動。

### 強化（board.py）
- 自然排序：epic/story 改用 `_natkey`，E2 排在 E10 前、E3 前 E3b（STATUS 已重生）。
- 新守衛：stories/ 內任何 .md 解析不到 id（壞掉/被截斷的 story 檔）→ ERROR。
  正好覆蓋「關機把 story 檔寫到一半」的情境。

### 文件
- ROLLBACK §二：標明 board --check 的 exit code（0/1）可驅動自動化；新增壞檔錯誤處置。
- ROLLBACK §五：新增「快照與 worktree 的位置」——並行期快照寫在該 worktree 的 dev/snapshots/，
  復原前要 cd 進 worktree；合併回 main 即該 story 在 main 的 known-good。

## 稽核結果（壓力測試全綠）
- exit code：壞 story=1、drift=1、乾淨=0（復原協定/CI 可依賴）。
- restore 精確度：改→還原、刪→復活、快照後新增→清除，三者皆正確。
- verify 抓竄改：sha256-mismatch 正確報出。
- latest-good：無 known-good 回空字串 exit=1；有則回最新 post。
- gen_stories 冪等：重跑建立 0、跳過 93。
- board --check：72 stories、0 errors、0 warns、一致。

## 已知取捨（非缺陷，已記錄）
- restore 非交易式：複製途中被砍可能留半狀態，但快照不可變，重跑 restore 即可冪等復原。
- 並行期快照為各 worktree 本地（見 ROLLBACK §五），靠 STATUS.worktree 欄索引。

## 狀態
全 story 仍 todo；CONTRACTS 仍 DRAFT；未開並行。下一步同 session-01：認領 E0-S1。
