# Journal — 2026-06-01 · session-14（後端重構：Narrative Progress Kernel）

## 重構完成 ✅ — 階段 S（7/7），整體 32 工單 31 done
整合使用者交付的穩定化補丁：world-state 由 ProgressKernel + PatchValidator 決定，story 只 realize。
旁路增量、feature-flag、單一分流、失敗回退、結構化 log。

## 工單
- SK01 progress_models + PatchValidator（修補丁 npc 點分 id bug：rsplit）。7 測。
- SK02 SceneGraphProvider 介面 + StaticOpeningSceneGraphProvider + ProgressKernel + data/opening_scene_graph.json。6 測。
- SK03 ContextBuilder（防暴雷最小檢視）+ progress_bridge（GameState↔Blackboard）。4 測。
- SK04 **beat loop 整合**：ENABLE_PROGRESS_KERNEL 預設 ON；step 單一分流 _step_kernel/_step_legacy；
  graph/kernel/validation 失敗 → log + 回退 legacy；結構化 log；story SKILL 追加 Narrative Progress Contract；
  run_story 加 context_override。7 測（ON 不重複開門/≥1 delta/NPC spawn/防暴雷/graph-fail fallback/OFF legacy）。
- SK05 clue/inventory/npc 進 snapshot + game_meta.progress_state；get_debug_state API + debug modal。3 測。
- SK06 30-beat scripted 回歸 + 指標。1 測。
- SK07 HUD 顯示 committed_event/progress_delta（onProgressInfo）。1 測。

## 驗證
- **30-beat 回歸指標**：progression_rate=1.0、repetition_rate=0.0、npc_presence_rate=0.967、open_door_count=1、0 failures。
- **真實 LLM kernel 流程（你的 key）**：「打開病房門」→ event.open_door_101 → story 描寫門已開+進走廊+抓痕（**不再問開門**）；
  「往走廊走」→ event.nurse_shadow_corridor → 護士黑影出現。防暴雷維持。
- 全套件回歸綠燈；board --check 32 工單 0 errors。ON/OFF/graph-fail 都不崩。

## 注意
- SK04 修改了既有檔（story.py/orchestrator_loop/webview_app/streaming.js/constants/skills story）；這些屬 SK 鏡像範圍。
  既有 U13/U15/U16/U17 的 last_good 仍指向 pre-kernel 狀態（較舊 known-good）；要完全回到 kernel 前，設 ENABLE_PROGRESS_KERNEL=false 即可（資料保留）。
- test_loop/test_webview_api 已 pin use_kernel=False（legacy 流程測試）；kernel 流程測試見 test_progress_integration。

## 現況
後端重構落地。剩 MVP-B（UB1–UB5）未動。GeneratedSceneGraphProvider（setup 依主題生後續區域）留 Patch 2。
