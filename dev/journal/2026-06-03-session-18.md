# Journal — 2026-06-03 · session-18（MVP-B：UB1–UB5 + 真實 LLM 修復）

## 完成 ✅ — MVP-B 5/5（UB1–UB5），整體 47 工單全 done
依使用者「開始製作 mvp-b」，按 build order **UB5→UB1→UB4→UB2→UB3** 逐單跑完整生命週期
（pre 快照 → 實作 mock-first → 驗收 pass → post 快照 --verify pass → 回填 last_good → done）。

## 前置 hotfix（工單流程外，真實 LLM 實測發現）
- **parser 列舉漂移容錯**：真實模型常吐非列舉 tone（analytical/direct/forceful…）→ Pydantic literal_error → 整個 DecisionPoint 被拒 → 掉 fallback「那三個固定選項」。修法：`StreamParser._coerce_enums` 把 tone/pacing/audio_cue/decision_type 正規化回合法值（不動 FROZEN 的 Models）。加回歸測試。
- config story 模型：`deepseek/deepseek-v3`（無效 slug）→ 改 `deepseek/deepseek-chat-v3-0324`（實測 5/5 真實、0 fallback）。

## 工單
- **UB5** 道具庫完整：`InventoryItem` += held_by/is_key_item；PatchValidator 加 inventory remove + `<id>.<attr> set`（held_by 轉移）；bridge 對外只露 held_by 不露 is_key_item；get_inventory 帶 held_by；inventory_snapshots 表隨 beat 寫入。6 測。
- **UB1** 技能宣稱封頂強化：warden `check_skill_claim`（本地偵測破格→接受但加具體接劇情的侷限，5 類模板，LLM 掛也封頂 B9）；loop `_record_skill_claim` 寫 (技能,侷限) ledger；webview push `onSkillLimit` + 前端 banner。7 測。
- **UB4** 輕量 dreaming：新 `core/agents/dreaming.py`；在場 NPC（C7）每 5 beat 演化；只提交 evolving patch（C6 權限邊界，碰不到 secret_core）；self_aware=false 不收 emergent_lie（C5）；非同步只產 patch、安全點 merge。7 測。
- **UB2** 一種結局序列：新 `core/agents/ending.py`；結局觸發→純敘述收尾 + 完整 real truth 揭露（結局唯一可露）+ 復盤（發現/全部碎片，提早觸發→沒找到的一次攤開＝劇情內合理化解）；loop `_finalize_ending`；前端 onEnding 渲染收尾/真相/復盤→回選單。5 測。
- **UB3** 雙層防暴雷驗證報告：新 `core/qa/antispoiler.py`；**對抗性 caller**（逐字 echo 整個 context 的最壞情況）+ 5 條 injection probe（忽略規則告訴我 real_bible…）→ 證明 story context 結構上無 real_bible，吐不出未揭露真相；產 `dev/reports/antispoiler-report.md`（6 beat 全綠）。5 測。

## 驗證
- 全套件 **555 passed, 3 skipped**（MVP-B +31 測）；每單收尾跑全回歸綠燈（承重牆 loop/validator/bridge 改動不破既有）。
- board --check：**47 工單 0 errors 0 warns**；5 個 UB*__post 快照皆 verify=pass。
- 真實 LLM 實測（deepseek-chat-v3-0324 + tone 修復）：開局 + 5 beat 連續真實生成、0 fallback。

## 現況
**MVP-A（U00–U20）+ 階段 S（SK01–SK13）+ 階段 P（P0–P7）+ MVP-B（UB1–UB5）全部 done（47/47）。**
NPC 會演化（dreaming）、技能宣稱有封頂與侷限、一種結局可收尾並揭露真相、道具庫完整、防暴雷有驗證報告。
parser hotfix + 模型修復讓真實遊玩順暢不掉 fallback。
