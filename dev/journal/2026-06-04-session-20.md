# Journal — 2026-06-04 · session-20（準備階段 N：敘事控制工單）

## 動作 — 依 patch `nightmare-assault-mvp-b-narrative-control-patch-v0.1` 準備工單（不開發）
使用者要求：依此 docs-first patch「按工作流幫我準備 story」。讀完 docs/00–11 + task_cards P0–P7 + reference_code，建工單上板。

## 決策
- 此 patch 是 MVP-B 之上的**敘事控制層**（旁路、flag OFF 預設），refine UB6（開場）與 UB7（masked 結局）——非取代。
- patch 內部用 P0–P7，與既有「階段 P（配置中心）」的 P0–P7 **id 衝突** → 改用 **NC0–NC7 / 階段 N**。
- 8 工單依 task cards 的目標/完成條件寫驗收；apply order NC0→…→NC7。

## 變更（純準備，無遊戲程式碼）
- 新增 8 工單 `dev/stories/NC0–NC7.md` + `dev/stages/stage-N.md`。
- `dev/CONTRACTS.md §十二`（新增，additive）：`NarrativeContract` / `OpeningBlueprint` / `RevealLadder` / `StoryAgentDelta` / `QualityGate` / `EndingGate`，canonical 指 patch docs/reference_code。
- `dev/tools/board.py`：`_stage_num` 加 N→10；STAGE_TITLE 補 N。
- `dev/PARALLEL-PLAN.md`：§二排程補 N 列、§三索引補 N + stage-N.md。

## 工單摘要
NC0 契約凍結+flag｜NC1 setup→NarrativeContract｜NC2 OpeningDirector（開場元素≤budget、最多 hinted）｜
NC3 Story Agent 降權（只執行 blueprint）｜NC4 RevealLadder（hidden→…→actionable 不跳級）｜
NC5 QualityGate（規則檢查+repair/fallback）｜NC6 EndingGate（因果門檻、0/8 不可 clean escape）｜NC7 Config hooks（optional）。

## 驗證
- `board.py --check`：**57 工單 0 errors 0 warns**（6 新契約全解析）。階段 N 渲染於 STATUS 末段，8/8 todo。
- 未動任何遊戲 runtime 程式碼。

## 現況
49 工單（含 MVP-B UB1–UB7）done + 階段 N（NC0–NC7）全 todo，可依 PARALLEL-PLAN §四「做階段 N / 做工單 NC0」認領；起點 NC0（無前置）。
