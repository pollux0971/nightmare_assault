# Journal — 2026-06-01 · session-09（階段 3：核心 agent 真接）

## 階段 3 — 完成 ✅（3/3，整體 13/25，MVP-A 13/20）
工線 D 三工單，**Sonnet 子 agent 並行**（本次 socket 穩定，3/3 成功）；我（Opus）驗收+整合。

### 工單
- `U09` setup：個性程式碼擲骰（speech_rhythm×emotional_base×quirk）+ run_setup 呼 LLM 生 SetupOutput → 寫 Blackboard（real_bible/npc/scene_registry/protagonist）+ 回開場。**B8 不降級**（失敗往上拋）。12 測+1 smoke。
- `U10` orchestrator：揭露閘門，程式碼判 min_beats/location_reached/requires_touched（AND），requires_semantic 才呼 LLM；搬碎片 real→revealed + newly_revealed。30 測。
- `U12` warden：**B9 三段降級**——本地硬規則 → LLM 語義 → 全失敗保守正常推進。22 測。

### 親驗關鍵不變式
- **B9**：踩硬規則 + LLM 全掛 → rule_violation=True、ending=death_physical、且**完全沒呼 LLM**；安全動作 + LLM 掛 → 保守不誤殺。
- **orchestrator**：beat2 揭露 0、beat3 揭露 1；**real_bible 不變**；revealed 含碎片（story 結構上拿不到 real）。

### 整合期發現 + 修正（重要）
- 真實 setup 首跑失敗：LLM 把 JSON 包在散文/```圍欄裡，`SkillCaller.call` 的裸 `json.loads` 解不開。
- **修正**：`core/agents/base.py` 加 `extract_json()` 容錯擷取（去圍欄、取 {…}、修智慧引號/尾逗號），所有結構化 agent 共用。U07 重新驗收+known-good 快照。
- **真實 LLM 世界生成成功** ✅（anthropic/claude-3.5-sonnet via OpenRouter，你的 key 經 env 注入）：
  廢棄精神病院主題 → 合法 SetupOutput：真相（非人道實驗）、3 NPC（self_aware [T,F,T]）、3 場景、氛圍開場。
  **這是第一個 AI 生成的可玩恐怖世界，全管線打通（SkillCaller→OpenRouter→setup→Blackboard）。**

### 驗證
- 各工單 mock 測試綠燈；全套件回歸綠燈；board --check 25 工單 0 errors。
- 真 API 驗證：U06 smoke + setup 真實世界生成皆成功。

## 現況
- 進度 13/25（階段 0–3 done）。下一步：**階段 4（story+compactor）**：D:`U13` story+串流（防暴雷，承重牆級）、`U14` compactor 30 beat（承重牆）。
- U13 會把 story 串流接 U08 StreamParser；U14 是無限模式命脈，建議 Opus 子 agent。
