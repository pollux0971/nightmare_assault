# Journal — 2026-06-05 · session-23（敘事控制 v0.2 補丁：建工單 + 同步設計）

## 任務
使用者提供新補丁 `nightmare-assault-mvp-b-narrative-control-patch-v0.2`（docs-first），要求「按照工作流更新 story + 同步內容到 design-fixed」。
**未要求開發**——本 session 只做工單化 + 契約 + 設計同步，開發留待使用者下指令（並先過契約認可閘門）。

## 背景（補丁緣由）
v0.1（階段 N）selfplay 證實開場收斂，但暴露更底層斷鏈：玩家調查（kernel clues / npc-chat hints）無法轉成 real_bible 真相進度 → 結局永遠 `0/X`。
補丁核心：受控橋接 `Evidence → Reveal Level → RevealedBible → Recap`，外加 npc-chat 敘事控制、答債、結局表層變體、兩段式逃脫、母題冷卻、動機心跳、表層消毒。

## 產出
- **新階段 R**（敘事控制 v0.2 · 揭露橋接）：board.py `_stage_num` 加 `R→12`、STAGE_TITLE 加 R；`dev/stages/stage-R.md`。
- **8 工單 NR0–NR7**（全 `todo`）：
  - NR0 RevelationBridge（D；deps U10/SK11/UB7）｜NR1 NPCChat 敘事控制（D；NR0/MC1/NC4）｜NR2 Answer Debt（D；NR1/U13）
  - NR3 EndingGate Surface Variant（D；NC6/UB7）｜NR4 Escape Commit Gate（D；NR3/SK13）
  - NR5 Motif Cooldown（F；NC5/NC3）｜NR6 Motive Heartbeat（D；NR0/NC1）｜NR7 SurfaceTextSanitizer（F；U13/MC1）
- **新契約 `dev/CONTRACTS.md §十四`**（10 條）：EvidenceEvent / RevelationBridge / RevealLedger / EvidenceMapping / NPCChatControl / AnswerDebt / EndingSurface / EscapeCommitGate / MotifCooldown / MotiveHeartbeat / SurfaceTextSanitizer。
- **PARALLEL-PLAN**：§二 加階段 R 排程列、§三 加階段 R/C 索引與 stage 檔指標。
- **設計同步 design-fixed**：新增 `13-narrative-control-v2.md`（完整 v0.2 規格）；`09-revision-notes.md` 補齊補丁四（v0.1 N）/五（MVP-C C）/六（v0.2 R）里程碑（先前漏記 N、C）。

## 驗證
- `board.py --check`：**70 工單（62+8），0 errors，0 warns**——8 條新契約全部在 CONTRACTS.md 解析得到、無前向依賴、STATUS.md 一致。
- 未動任何遊戲程式碼（符合使用者「只更新 story + 同步設計」範圍）。

## 紀律備註
- 契約為**新增 §十四**（非改 FROZEN 既有契約）；旁路受 `ENABLE_NARRATIVE_CONTROL` 控管、預設行為不變、story/npc 永不見 real_bible 不變、refine（非取代）UB7/§十二 RevealLadder。
- docs-first：canonical 在補丁包 docs 00–10 / task_cards / reference_code（範例不照抄）。

## 現況 / 下一步
- 工單已上板（todo），設計已同步。**等待使用者認可 §十四 契約並下「做階段 R」** → 才開 worktree 並行開發（apply order NR0→…→NR7）。
- 整合驗收主線：玩家檢查紙條 + 問 NPC 頻率 + 查文件後 recap 不得 0/X；0 confirmed 逃脫渲染 ambiguous_escape；「我試圖離開」先出口候選；重複提問付答債。
