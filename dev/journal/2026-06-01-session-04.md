# Journal — 2026-06-01 · session-04（開發模型策略）

## 本次工作
依使用者要求,讓工作流可切換開發期 Claude 模型:主 agent Opus、子 agent 以 Sonnet 為主。

## 釐清
- 未啟動 Workflow 編排:目前所有 story 仍 todo、未呼叫任何階段,沒有可編排的工作。
  本需求是「把模型策略寫進工作流」,屬 config/doc,故只改 dev OS 文件。

## 產出
- PARALLEL-PLAN.md 新增 §六「模型策略（可切換）」:
  · 主 agent=Opus(整合/監看/決策/回滾);子 agent=Sonnet(預設)。
  · per-track 預設表:承重牆 D-compact(E3b)/C-frontend(E7-F2)/foundation(E2/E3) 建議升 Opus,其餘 Sonnet。
  · 切換方式:持久(改表)或一次性(呼叫階段時口頭覆寫)。
  · 機制:fan-out 用 Agent 工具帶 model:"sonnet";主迴圈維持 Opus。
- PARALLEL-PLAN §四 step3 + 抬頭、CLAUDE.md §6 並行紀律 各加模型策略。
- 明確區分「開發期 Claude 模型」vs「遊戲執行期 OpenRouter agent 模型(Heavy/Medium/Light)」,兩者不同層。

## 狀態
全 story 仍 todo;CONTRACTS 仍 DRAFT。下一步同前:可呼叫「做階段 0」。
