# Journal — 2026-06-01 · session-01（開帳）

## 本次工作
建立 Nightmare Assault 的**開發作業系統**（尚未寫任何遊戲程式碼）。

### 產出
- `git init`（僅供 worktree 並行隔離；回滾不靠 git）、`.gitignore`。
- `dev/tools/snapshot.py` — 純檔案快照（snapshot/restore/list/verify/latest-good），回滾骨幹。
- `dev/tools/board.py` — 從 `dev/stories/*.md` 重算 `dev/STATUS.md` + lint（`--check` 抓漂移/懸空依賴）。
- `dev/tools/gen_stories.py` — 冪等骨架產生器（不覆寫手改）。
- `dev/CONTRACTS.md`（**DRAFT**）— 27 個契約 id，含 Blackboard/權限表/SignalBus/LLM·Skill/持久化/前端。
- `dev/epics/`（21 檔）、`dev/stories/`（72 檔，Phase 1+2 全部 + Phase 3 epic stub）。
- `dev/WORKFLOW.md`、`dev/ROLLBACK.md`、`dev/STATUS.md`。
- 專案根 `CLAUDE.md`（開發鐵律，OVERRIDE）。
- 遊戲程式碼骨架目錄（core/ ui/ skills/ config/ …，皆空）。

### 已驗證
- 快照 pre→改檔→restore→還原→verify 全通過。
- `board.py --check`：72 stories，0 errors，0 warns，STATUS 一致。

## 目前狀態
- 所有 story `status: todo`。CONTRACTS.md 仍 **DRAFT**。
- **尚未開並行**（閘門：E0+E1 完成、CONTRACTS 轉 FROZEN 後才開 worktree）。

## 下一步（給下一個 session）
1. 跑開機復原序列（CLAUDE.md §1）。
2. 從 `foundation` track、Wave 0 開始：認領 **E0-S1**（其 depends_on 為空）。
3. E0→E1 走完後，把 `dev/CONTRACTS.md` 依實作定稿、狀態改 **FROZEN**，再依 WORKFLOW §三 開並行 worktree。

## 待決（實作時記實測值）
- dreaming K（在場5/離場3K，E9-S5）｜emergent_lie 強度（中等，E9-S3）｜命運表權重（E9b-S3）｜聊天計數（E8-S3）。
