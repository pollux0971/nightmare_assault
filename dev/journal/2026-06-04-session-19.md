# Journal — 2026-06-04 · session-19（MVP-B patch：序幕鉤子 + 結局揭露政策）

## 完成 ✅ — UB6 + UB7（依使用者實機回饋新增工單），整體 49 工單全 done
依使用者設計回饋（`12-mvp-b.md §一–十`：開場缺一個夠強的核心疑問、結局不該一次攤全部）新增 2 工單，
**嚴格按工作流**：建工單上板（board 49 工單）→ 逐單 pre 快照 → in_progress → 實作 mock-first → 驗收 pass →
post 快照 --verify pass → 回填 last_good → done → board → journal。

## 工單
- **UB6** 序幕鉤子 + 真相種子：新 `core/agents/opening.py`。
  - `build_opening_seeds`：從 real_bible/protagonist 組 5 型種子（personal/mechanical/true/false/imagery），
    每個 seed 拆 **surface（可餵 story）** 與 **hidden_truth（留 real_bible，永不餵 story）**。
  - `build_opening_context`：注入 5 條開場義務 + 表層種子 + 長度政策（600–900 字）；loop `_kernel_intro_beat` 富化。
  - `skills/story/SKILL.md` 加「序幕特別規則」（動機鉤子/真假混合異常/身份鉤子/表層想像/停在第一選擇；禁直接解釋真相）。
  - **防暴雷斷言**：開場 context / 表層種子不含 hidden_truth / real_bible。6 測。
- **UB7** 結局揭露政策（masked）：`ending.py`。
  - recap 的 discovered/missed 改帶 {id,title,content}；`render_ending_text(ending, mode)`。
  - **masked**（預設）：已發現露全文、未發現只露遮罩標題 + ？？？ + 重玩鉤子；核心真相僅在發現比例 ≥0.6 才揭露。
  - **full**（debug）：全揭。前端 onEnding 改 masked 分區（已確認/未確認）。
  - 調整既有 UB2 測試對應新政策（render 全揭改 mode="full"、recap 改 dict）。7 測。

## 新契約
`dev/CONTRACTS.md`：新增 `OpeningSeeds`（surface/hidden 分離，防暴雷）；`EndingConditions` 補 masked 政策註記。

## 驗證
- 全套件 **567 passed, 3 skipped**（+12 測）；每單收尾全回歸綠燈（loop intro / ending 改動不破既有）。
- board --check：**49 工單 0 errors 0 warns**；UB6/UB7 post 快照 verify=pass。

## 動機（使用者實機回饋）
上一場真實 playthrough：事件推進/NPC/技能封頂/死亡結局都 OK，但開場偏一般恐怖開局、真相到死才一次攤開。
UB6 讓序幕先建立「不對勁的核心」（真假混合鉤子，不暴雷）；UB7 讓結局只揭露玩家走到過的真相、其餘留白給重玩。

## 現況
49 工單全 done。設定提醒：`config.json` 已補 `dreaming: qwen/qwen3-max`（UB4 可在真實遊玩觸發）。
