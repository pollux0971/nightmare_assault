# Journal — 2026-06-01 · session-12（階段 6：前端 + MVP-A 打磨）

## 階段 6 — 完成 ✅（4/4，整體 20/25）— **MVP-A 20/20 = 100%**
前端工線 E。整合者（我，Opus）親自實作（前後端串流橋接、threading 整合風險高，且使用者要實機測試）。

### 工單
- `U16` pywebview 骨架 + API：`webview_app.py`（API class，13 方法，**key 永不到前端**、背景 thread 串流）、`main.py`（lazy import webview）、`ui/index.html`、`ui/css/theme.css`（深色恐怖）、`ui/js/views.js`/`api.js`。
- `U17` 串流渲染（承重牆）：`ui/js/streaming.js` —— NA.appendToken/onContinue/onDecision/onStatus/onError/onBeatComplete/onOpening/onEnding；控速吐字、關鍵詞血紅、選項逐個淡入、敘述吐完才呈現決策。**前端不解析分隔符/JSON**（D4）。
- `U18` 主畫面 + 前置：設定（key 輸入→存 config.json）、主選單、新局、載入（序章儀式）、狀態機 busy/disabled/error。
- `U19` 存檔 + 道具：存檔點 modal、道具面板（不洩漏 is_key_item）。

### 整合增強
- `run_story` + `BeatLoop.start/step` 加 `on_event` 回呼 → 前端逐 token 串流。U13/U15 刷新 known-good。

### 驗證（無頭，無 GUI/display）
- `tests/test_webview_api.py` 8 passed：FakeWindow 驗證 NA.* 推送契約（開局推 onOpening/appendToken/onDecision/onBeatComplete/onStatus；step 串流+前進；warden 結局→onEnding；道具不洩漏 key_item；config 往返）。
- JS 三檔 `node --check` 過；`webview_app` 無 pywebview 可 import；全套件回歸綠燈；board --check 25 工單 0 errors。
- pywebview 本機未裝（需系統 GUI）→ 使用者實機測試；視覺驗收（起窗/串流節奏/不跑版）待使用者確認。

### 使用者測試準備
- `config/config.json` 預植使用者 OpenRouter key（gitignored，不進 git/快照）；`config/config.example.json` 供他人。
- `RUN.md`：安裝（`pywebview[qt]`）、`python main.py`、玩法、六項驗收觀察點、疑難。

## 現況
- **MVP-A（U00–U19）20/20 完成**。後端核心 + 前端齊；真實連續遊玩已於 session-11 驗證可玩。
- 待使用者實機視覺驗收（六項）。剩 **MVP-B（UB1–UB5）**：技能封頂強化、一種結局、防暴雷報告、輕量 dreaming、道具庫完整。
