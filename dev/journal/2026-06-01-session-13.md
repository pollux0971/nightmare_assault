# Journal — 2026-06-01 · session-13（等待動畫 + HUD 生成進度）

## 使用者需求
1. 故事還沒生成前一直跑等待動畫；2. HUD 風格 to-do list 顯示目前生成到哪一步。

## 變更（MVP-A 打磨，跨 U15/U16/U17）
- 後端進度事件：`BeatLoop.start/step` 加 `on_progress` 回呼（setup→orchestrator→story；step：warden→orchestrator→story）；
  `start` 加 `on_opening`（序章先於第一個 beat 推出，修正原本順序）。
- `webview_app`：`_on_progress` 推 `NA.onProgress({stage,label})`；stage 標籤 建構世界真相/守門人裁決/揭露閘門/編織夢魘。
- 前端：
  · **HUD**（固定右下）：`#hud` 清單，onProgress 逐步標 ●active→✓done，onBeatComplete 收起。
  · **等待動畫**：submit 後在 #narrative 注入血點脈動 + 循環恐怖台詞，直到第一個 appendToken 才隱藏。
  · 新局：loading 儀式 + HUD；onOpening/appendToken 觸發 loading→game 切換（switchToGameIfLoading）；onError 在 loading 時回新局畫面提示。
- 修：pywebview 後端在 Linux 自動選 qt（避開需 gi 的 GTK）。

## 驗證
- JS 三檔 node --check 過；`test_webview_api` 8 passed（新增 onProgress + HUD 標籤斷言）；全套件回歸綠燈；board 25 工單 0 errors。
- U15/U16/U17/U18 known-good 已刷新。

## 現況
MVP-A 20/20；本次為打磨。等使用者實機確認等待動畫與 HUD。其餘 MVP-B（UB1–UB5）未動。
