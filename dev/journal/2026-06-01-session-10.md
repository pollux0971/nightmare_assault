# Journal — 2026-06-01 · session-10（階段 4：story + compactor）

## 階段 4 — 完成 ✅（2/2，整體 15/25，MVP-A 15/20）
兩個關鍵工單交 **Opus 子 agent 並行**（防暴雷 + 承重牆），我（Opus 整合者）親驗關鍵不變式。

### 工單
- `U13` story+串流（防暴雷）：build_story_context **白名單投影**只放 revealed + 安全欄位；在場 NPC 公開面（剝 secret_core/self_aware/emergent_lies/revealed_layers）；玩家輸入 `<player_action>` 包裝 verbatim；串流接 StreamParser → DecisionPoint；assert_no_spoiler 斷言。7 測+1 smoke。
- `U14` compactor+30beat（承重牆）：BeatWindow/RollingSummary（**取代語意→有界**）/FactLedger（protected 不刪）；Compactor 依 usage_pct 分級壓縮、保護清單聯集（伏筆不滅）；apply_to_blackboard 走 submit_patch（A5 非同步隔離）。29 測。

### 親驗的 MVP-A 關鍵不變式（三大之二）
- **防暴雷（E2/C2）結構性**：把機密真相同時埋進 real_bible.what_really_happened、NPC.secret_core、evolving.emergent_lies → build_story_context 後 story context **完全不含**任一禁字。不是 prompt 自律，是資料隔離 + 白名單投影。
- **30 beat 有界（E3）**：window 30 beat 後最大 = 6（≤BEAT_WINDOW_SIZE）；摘要 token 估算恆定（取代非串接），遠在 cap=1000 內；伏筆 24+ 次壓縮後仍在保護清單；ledger 硬事實不滅。

### 三大承重牆現況
- U08 StreamParser ✓｜U13 story 防暴雷 ✓｜U14 compactor 30beat ✓ —— MVP-A 三大關鍵（防暴雷/30beat/JSON 穩定）的底層全部就位。

### 注意點（子 agent 已自洽處理）
- compactor SKILL.md 範例把 ledger_updates 寫成二元組，但 CompactorOutput 是 list[LedgerFact]。子 agent 以 models.py 為準，內部容忍 LedgerFact/dict/tuple 三態，與真實 LLM 二元組輸出相容。

### 驗證
- U13 7+1skip、U14 29，全套件回歸綠燈；board --check 25 工單 0 errors。

## 現況
- 進度 15/25（階段 0–4 done）。下一步：**階段 5（beat 主迴圈）**：A:`U15` 把 warden→orchestrator→story→快照→安全點 merge→compactor 串成迴圈。
- U15 是整合者（我）的工單、整合風險最高、序列為主不過度並行。完成後**後端 MVP-A 核心通**，可跑真實連續 beat。
