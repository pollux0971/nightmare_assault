# Journal — 2026-06-02 · session-15（Patch 2：主題化場景圖）

## 完成 ✅ — 階段 S 9/9（SK08+SK09），整體 34 工單 33 done
GeneratedSceneGraphProvider：用程式碼模板 + setup 主題內容編譯主題化推進圖。kernel 對任何主題都能驅動推進。

## 工單
- SK08 `build_themed_graph(bb_snapshot)` + `GeneratedSceneGraphProvider`：結構模板（exit→second→meet npc→escalation，schema 同開場圖），
  內容主題化（setup 的 scene_registry/npc_registry/atmosphere）；缺漏 synthesize 預設，永遠有效。含中文 npc id（validator rsplit 已支援）。4 測。
- SK09 BeatLoop 接圖：kernel 初始化移到 start() 之後，依 setup 輸出建 provider；失敗鏈 **explicit→generated→static→legacy**，皆 log。
  更新 kernel 整合測試（預設改 generated graph：event.exit_start/scene.beyond/clue.first_sign/npc.<name>）。

## 驗證
- 30-beat 回歸 **static 與 themed 皆達標**：progression=1.0、repetition=0.0、npc=0.967、exit_event_count=1、0 failures。
- **真實 LLM 非醫院主題（深海研究站）**：setup 生 starting_room/main_corridor/張潤 → 「打開艙門前進」→ event.exit_start → 主走廊 + 暗紅色液體線索（不再問開門）→「觀察」→ 張潤遠處出現。
- 全套件回歸綠燈；board --check 34 工單 0 errors。fallback 鏈（generated/static/legacy）皆測。

## 設計選擇
- **不讓 LLM 生 raw graph JSON**（schema 風險高）；改「程式碼模板 + LLM 主題內容」。結構穩定、內容主題化。
- 更深的主題專屬分支結構（非單一模板）留 Patch 3。

## 現況
Narrative Progress Kernel + 主題化場景圖 全部落地。剩 MVP-B（UB1–UB5）。
