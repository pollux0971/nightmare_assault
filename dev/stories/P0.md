---
id: P0
stage: P
lane: A
title: 配置中心契約凍結（名稱/profile/fragment key/flags/fallback）
status: done
worktree: -
depends_on: []
contracts: [PromptFragments, FeatureFlags, ConfigPromptSource]
last_good_snapshot: P0__post__20260603-173005
owner_session: -
---

# P0 · 配置中心契約凍結

- **階段**：P（配置中心 / Story Agent 模組化 · patch v1.1）　**工線**：A（整合者 / Tech Lead）
- **依賴**：無（進入條件：MVP-A U00–U20 + 階段 S 全 done）　**契約**：PromptFragments, FeatureFlags, ConfigPromptSource

## 目標 / 範圍
docs-first：在動任何 runtime 前，**凍結配置中心的名稱與契約**，止住介面漂移。不寫 runtime 程式碼。
凍結五件：agent 名、profile 名、fragment key 命名、feature flags、prompt fallback 政策。

## 對應來源
patch `docs/00-executive-intent.md`、`docs/01-apply-order.md §P0`、`task_cards/P0_contract_freeze.md`；表格 `docs/04`、fragment 文字 `docs/05`。
dev/CONTRACTS.md §十一（本批已先補配置中心契約索引，canonical 指 patch docs）。

## 實作步驟
- 建凍結清單 `config/CONTRACT_FREEZE.md`（或 dev note），逐條列：
  - agent 名：`setup` / `story` / `warden` / `orchestrator` / `compactor`
  - profile 名：`mvp_a_safe` / `debug` / `creative` / `low_cost` / `strict_kernel`
  - fragment key 命名 `<agent>.<purpose>`；story 八件（role/objective/kernel_obedience/no_repetition/open_choice/context_policy/output_format/style_horror）
  - feature flags：`ENABLE_CONFIG_CENTER`(P4 開) / `ENABLE_PROMPT_PREVIEW` / `ENABLE_RUN_CONFIG_SNAPSHOT` …（含預設值）
  - fallback 政策：active profile → default profile → static built-in（config 失敗 log + 退 static）
- 核對 dev/CONTRACTS.md §十一 八個契約 id 與本清單一致（無歧義）。
- **不改任何 runtime 檔**。

## 驗收（可執行 — pass 才算 done）
- [ ] `config/CONTRACT_FREEZE.md` 存在，五類名稱齊全且與 patch docs/04、docs/05 一致（無新增臆造名）。
- [ ] `python3 dev/tools/board.py --check`：本階段所有 `contracts:` 都指得到 CONTRACTS.md（0 dangling-contract warns）。
- [ ] `git diff --name-only` 僅含 docs / 契約 / 清單檔，**無 core/ 或 webview 等 runtime 檔變更**。

## 回滾備註
純文件凍結，無 runtime 下游污染；還原 pre 快照即回原狀。

## 認領紀錄（執行時填）
- 開工：填 owner_session / worktree → `snapshot.py snapshot P0 pre`
- 完成：驗收 pass → `snapshot.py snapshot P0 post --verify pass` → 回填 last_good_snapshot
