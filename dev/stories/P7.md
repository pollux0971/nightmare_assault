---
id: P7
stage: P
lane: D
title: 擴展配置中心至其他 agent（warden/orchestrator/compactor/setup）
status: done
worktree: -
depends_on: [P6, P5]
contracts: [PromptFragments, AgentContextPolicy]
last_good_snapshot: P7__post__20260603-180546
owner_session: -
---

# P7 · 擴展至其他 agent（Story 穩定後才做）

- **階段**：P（配置中心 / Story Agent 模組化 · patch v1.1）　**工線**：D（Agent Logic）
- **依賴**：P6, P5　**契約**：PromptFragments, AgentContextPolicy
- **備註**：apply order 最後一站（「Later」）。Story Agent 證明穩定後，才把同一套配置機制套到其他 agent。

## 目標 / 範圍
把 P1–P6 的配置機制套到 **warden → orchestrator → compactor → setup**（依序，**一次只遷一個**，不一次全遷）。
**不動 real_bible 可見性規則、不弱化 story hidden-info 限制。**

## 對應來源
patch `docs/01 §P7`、`docs/02 §P7 guardrail`、`docs/05`（warden/orchestrator/compactor/setup fragments）、`task_cards/P7_other_agents.md`。

## 實作步驟
- 依序為 warden / orchestrator / compactor / setup 建 fragments + agent_context_policy + binding（每個獨立 PR/session）。
- 各 agent prompt 經 PromptComposer，prompt_hash 進 run snapshot。
- 保持 orchestrator 可讀 real_bible、story 永不可讀（context policy 強制）；setup 唯一不可降級不變。

## 驗收（可執行 — pass 才算 done）
- [ ] pytest tests/test_other_agents_config.py：warden/orchestrator/compactor/setup 各有 fragments + context policy；各 agent prompt_hash 進 run snapshot。
- [ ] real_bible 可見性規則不變（story `include_real_bible=False`、orchestrator 可讀）；既有 agent 行為回歸不破。
- [ ] 遷移以 agent 為邊界、一次一個（commit/session 不混遷多 agent）。

## 回滾備註
逐 agent additive；任一 agent 出錯只回滾該 agent；還原 pre 快照。

## 認領紀錄（執行時填）
- 開工：填 owner_session / worktree → `snapshot.py snapshot P7 pre`
- 完成：驗收 pass → `snapshot.py snapshot P7 post --verify pass` → 回填 last_good_snapshot
