---
id: U08
stage: 2
lane: C
title: StreamParser 三級 repair（承重牆）
status: done
worktree: -
depends_on: [U01]
contracts: [StreamParser]
last_good_snapshot: U08__post__20260601-125624
owner_session: -
---

# U08 · StreamParser 三級 repair（承重牆）

- **階段**：2（LLM 基礎 + agent 外殼）　**工線**：C（LLM Infrastructure / Parser（client/parser/SkillCaller））
- **依賴**：U01　**契約**：StreamParser

## 目標 / 範圍
core/llm/parser.py：逐 token 滑動視窗偵測分隔符、分離 narrative/decision、三級 repair、fallback decision UI。實作 07 §三全部。

## 對應設計章節（引用，不複製；契約見 dev/CONTRACTS.md）
07 §三、build/CONTRACTS §四、CHECKLIST B2/B3/B4/B5/B6

## 實作步驟
- feed(token)→事件；finalize()→DecisionPoint
- L1 程式碼修復/L2 LLM repair/L3 fallback UI

## 驗收（可執行 — pass 才算 done）
- [ ] 正常 JSON 過；缺逗號 L1 修復；分隔符被拆多 token 仍偵測（B2）；忘記 DECISION 走 fallback（B6）；narrative 已串流不回收（B3）；L1/L2/L3 各一測（B4）。**此不穩整個遊戲不穩**

## 回滾備註
獨立；還原 pre 快照。

## 認領紀錄（執行時填）
- 開工：填 owner_session / worktree → `snapshot.py snapshot U08 pre`
- 完成：驗收 pass → `snapshot.py snapshot U08 post --verify pass` → 回填 last_good_snapshot
