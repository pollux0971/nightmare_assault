---
id: U06
stage: 2
lane: C
title: OpenRouterClient + fallback + trace
status: done
worktree: -
depends_on: [U01, U02, U05]
contracts: [OpenRouterClient]
last_good_snapshot: U06__post__20260601-125624
owner_session: -
---

# U06 · OpenRouterClient + fallback + trace

- **階段**：2（LLM 基礎 + agent 外殼）　**工線**：C（LLM Infrastructure / Parser（client/parser/SkillCaller））
- **依賴**：U01, U02, U05　**契約**：OpenRouterClient

## 目標 / 範圍
core/llm/client.py call/stream、fallback 鏈、timeout；每次寫 llm_traces。

## 對應設計章節（引用，不複製；契約見 dev/CONTRACTS.md）
build/CONTRACTS §三、01 §三、08 §三、CHECKLIST B1/B7

## 實作步驟
- call(agent,system,user,temp,stream)/stream()
- fallback 鏈 + timeout
- 每次 call 寫 llm_traces

## 驗收（可執行 — pass 才算 done）
- [ ] 最小 prompt 真 call 成功；主模型失敗觸發 fallback；trace 有記錄（B1）；timeout 走 fallback（B7）

## 回滾備註
依賴 U01/U02/U05；還原 pre 快照。

## 認領紀錄（執行時填）
- 開工：填 owner_session / worktree → `snapshot.py snapshot U06 pre`
- 完成：驗收 pass → `snapshot.py snapshot U06 post --verify pass` → 回填 last_good_snapshot
