---
id: SK12
stage: S
lane: D
title: soft lookahead（每 beat 重算、非既定事實）
status: done
worktree: -
depends_on: [SK11]
contracts: [ProgressKernel, ProgressContext]
last_good_snapshot: SK12__post__20260602-165643
owner_session: -
---

# SK12 · soft lookahead（每 beat 重算、非既定事實）

- **階段**：S（穩定化補丁 · 敘事規劃強化）　**工線**：D
- **依賴**：SK11　**契約**：ProgressKernel, ProgressContext

## 目標 / 範圍
kernel 每 beat 依 open obligations + 當前場景其他事件重算 soft_lookahead（1–3 個可能方向），餵 story 為非綁定提示（不得當既定）。不存進 GameState。

## 對應來源
使用者指示：soft lookahead 可重算；story delta 第8條

## 實作步驟
- ProgressResult/kernel 加 soft_lookahead 計算（每次 resolve 重算）
- ContextBuilder 帶 soft_lookahead；story SKILL 已含『soft 不當既定事實』

## 驗收（可執行 — pass 才算 done）
- [ ] pytest tests/test_soft_lookahead.py：soft_lookahead 隨狀態改變而重算；不持久於 GameState；context 含之且標示非既定

## 回滾備註
旁路強化（flag 可關，fallback 鏈不變）；還原 pre 快照。

## 認領紀錄（執行時填）
- 開工：snapshot.py snapshot SK12 pre
- 完成：snapshot.py snapshot SK12 post --verify pass → 回填 last_good_snapshot
