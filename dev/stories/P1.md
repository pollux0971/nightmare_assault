---
id: P1
stage: P
lane: B
title: 配置表（additive SQLite + 種子預設）
status: done
worktree: -
depends_on: [P0, U05]
contracts: [ConfigSchema, AgentContextPolicy, SQLiteSchema]
last_good_snapshot: P1__post__20260603-173410
owner_session: -
---

# P1 · 配置表（additive SQLite + 種子預設）

- **階段**：P（配置中心 / Story Agent 模組化 · patch v1.1）　**工線**：B（Core State / Persistence）
- **依賴**：P0, U05　**契約**：ConfigSchema, AgentContextPolicy, SQLiteSchema

## 目標 / 範圍
依 docs/04 新增配置表（**additive only**，既有存檔不破壞），並種子 `mvp_a_safe` + story agent 預設。
story `agent_context_policies.include_real_bible=false`（硬規則，程式碼層）。

## 對應來源
patch `docs/04-agent-config-table-spec.md`（表規格）、`docs/01 §P1`、`task_cards/P1_config_tables.md`。
既有 SQLite：dev/CONTRACTS.md `SQLiteSchema`（08 §一）。

## 實作步驟
- 新增表（建在 U05 的 db.py，additive migration）：`config_profiles` / `agent_configs` / `prompt_fragments` /
  `prompt_fragment_versions` / `agent_prompt_bindings` / `agent_context_policies` / `feature_flags` /
  `run_config_snapshots` / `prompt_test_cases` / `prompt_test_results`（+ index/unique）。
- migration 冪等（IF NOT EXISTS / schema_meta 版本 bump）；既有 runs/beats/save_points/… 一律不改名不刪。
- 種子預設：`mvp_a_safe` profile（is_active）；story agent_config（temp 0.65–0.8、context_budget 5000–8000、stream true、schema `StoryBeatOutput`）；
  story 八個 fragment binding（sort_order 10/20/30/40/50/60/70/80）；story agent_context_policy（include_real_bible=false）。

## 驗收（可執行 — pass 才算 done）
- [ ] pytest tests/test_config_schema.py：建表為 **additive**（既有 runs/beats/save_points 結構不變、舊存檔可讀回）；migration 冪等（重跑不炸、版本不重複 bump）。
- [ ] 種子：`mvp_a_safe` 存在且 is_active；story agent_config 預設值符 docs/04；story agent_context_policy `include_real_bible=False`。
- [ ] 既有 app 仍可開（import core + 開一場既有存檔 smoke 不崩）。

## 回滾備註
additive 建表；還原 pre 快照即回 U05 schema（migration 可重入，不留半表）。

## 認領紀錄（執行時填）
- 開工：填 owner_session / worktree → `snapshot.py snapshot P1 pre`
- 完成：驗收 pass → `snapshot.py snapshot P1 post --verify pass` → 回填 last_good_snapshot
