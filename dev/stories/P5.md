---
id: P5
stage: P
lane: E
title: 配置 UI（draft→preview→activate；不直改 active）
status: done
worktree: -
depends_on: [P6, U18]
contracts: [ConfigUI, JsApi]
last_good_snapshot: P5__post__20260603-180335
owner_session: -
---

# P5 · 配置 UI（可視化編輯 + 預覽）

- **階段**：P（配置中心 / Story Agent 模組化 · patch v1.1）　**工線**：E（Frontend / pywebview）
- **依賴**：P6, U18　**契約**：ConfigUI, JsApi
- **備註**：apply order 刻意把 UI 排在 P6 之後 — 先有快照/測試再給可視化編輯，否則改 prompt 難 debug。

## 目標 / 範圍
加簡單配置 UI（不做完整 CMS）：agent config 表、prompt fragment 編輯器、prompt preview panel、feature flag panel。
**安全規則：編輯只進 draft，啟用（activate）後才影響 active run；preview 不呼 LLM；顯示 active profile + prompt hash。**

## 對應來源
patch `docs/01 §P5`、`docs/02 §P5 guardrail`、`docs/07-ui-wireframe-and-workflow.md`、`task_cards/P5_config_ui.md`。
既有：U18 主畫面/前置畫面、dev/CONTRACTS.md `API`/`JsApi`。

## 實作步驟
- 後端 API（JsApi 暴露）：list/get fragments、save draft、activate、preview(compose 不呼 LLM)、list/toggle feature flags、回 active profile + prompt_hash。
- 前端：config 視圖（agent config 表 + fragment 編輯器 + preview panel + flag panel）；編輯寫 draft、activate 才生效。
- preview 走 P2 PromptComposer preview 路徑（零 LLM）。

## 驗收（可執行 — pass 才算 done）
- [ ] pytest tests/test_config_ui_api.py：編輯 fragment 產 draft、不動 active run；activate 後才生效；preview API 不呼 LLM（mock `assert_not_called`）；回傳含 active profile + prompt_hash。
- [ ] JS 語法過；前端可檢視 story fragments、預覽編譯後 prompt、安全 toggle feature flag（draft→preview→activate 流程）。

## 回滾備註
純前端 + 配置 API（draft 不影響 active）；還原 pre 快照。

## 認領紀錄（執行時填）
- 開工：填 owner_session / worktree → `snapshot.py snapshot P5 pre`
- 完成：驗收 pass → `snapshot.py snapshot P5 post --verify pass` → 回填 last_good_snapshot
