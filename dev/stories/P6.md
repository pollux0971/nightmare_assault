---
id: P6
stage: P
lane: F
title: Run 配置快照 + prompt 回歸測試（開門/無暴雷/hash/線索/自由輸入）
status: done
worktree: -
depends_on: [P4]
contracts: [RunConfigSnapshot, PromptComposer]
last_good_snapshot: P6__post__20260603-174736
owner_session: -
---

# P6 · Run 配置快照 + prompt 回歸測試

- **階段**：P（配置中心 / Story Agent 模組化 · patch v1.1）　**工線**：F（QA / Test）
- **依賴**：P4　**契約**：RunConfigSnapshot, PromptComposer
- **備註**：依 apply order（P0→…→P4→**P6**→P5→P7），快照/測試先於 UI，避免「能改 prompt 卻無法 debug」。

## 目標 / 範圍
讓 prompt/config 可 debug：每場 run 存 active config 快照（profile / config_json / compiled_prompt_hash / enabled fragments），
並補 prompt 回歸測試集，能回答「哪個 prompt 版本造成這個行為」。

## 對應來源
patch `docs/01 §P6`、`docs/04`（run_config_snapshots / prompt_test_cases / prompt_test_results）、`docs/08`（run snapshot contract）、`task_cards/P6_snapshots_and_tests.md`。

## 實作步驟
- 新 run：對每個 enabled agent compile prompt → 算 prompt_hash → 存 `run_config_snapshots`（config_json + compiled_prompt_hash + enabled_fragment_keys，選存 preview）。
- 載入存檔：優先用該 run 的 config 快照（可重現）；遷移到當前 profile 須顯式。
- 回歸測試集（prompt_test_cases）：開門 anti-repetition、story 無 real_bible、prompt hash 決定性、new clue 出現在 beat、free input 保留。

## 驗收（可執行 — pass 才算 done）
- [ ] pytest tests/test_run_config_snapshot.py：新 run → `run_config_snapshots` 寫入 profile + config_json + compiled_prompt_hash + enabled_fragment_keys；載入存檔優先用該 run snapshot（可重現）。
- [ ] pytest tests/test_prompt_regression.py：五項全綠 — 開門不重複、story context 無 real_bible、prompt_hash 決定性、new clue 出現在 story beat、free_input 保留。
- [ ] 能由任一 run 反查其使用的 prompt/config 版本（hash 可對應回 fragment 集）。

## 回滾備註
新增快照表寫入 + 測試；additive；還原 pre 快照。

## 認領紀錄（執行時填）
- 開工：填 owner_session / worktree → `snapshot.py snapshot P6 pre`
- 完成：驗收 pass → `snapshot.py snapshot P6 post --verify pass` → 回填 last_good_snapshot
